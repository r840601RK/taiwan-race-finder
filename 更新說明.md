# 一鍵更新 events.json

雙擊專案根目錄的 `更新賽事資料.bat`，工具會整理並重寫 `outputs/events.json`。

如果要更新後直接上傳 GitHub，雙擊專案根目錄的 `更新並上傳GitHub.bat`。

目前工具會自動嘗試抓取：

- 運動筆記 Biji
- 全統運動 CTRun
- 伊貝特報名網

工具會：

- 保守合併新抓到的賽事。
- 保留既有賽事資料，不會因為某個網站失敗就清空資料。
- 重新排序賽事。
- 檢查必要欄位與日期格式。
- 更新 `updatedAt`。

注意：

- GitHub Pages 是靜態網站，網頁上的按鈕不能直接爬網站並改 GitHub 上的 `events.json`。
- 如果某個網站改版或阻擋爬蟲，該來源會顯示失敗，但不會破壞既有資料。
- 更新完本機 `events.json` 後，仍需要把新版檔案上傳到 GitHub，線上網站才會更新。

## 自動上傳 GitHub

`更新並上傳GitHub.bat` 會：

- 先執行賽事更新。
- 再把 `outputs` 裡的網站檔案上傳到 `r840601RK/taiwan-race-finder`。
- 上傳後 GitHub Pages 通常需要等 1 到 3 分鐘才會刷新。

第一次使用時需要貼上 GitHub Personal Access Token。

Token 權限至少需要：

- Repository：`r840601RK/taiwan-race-finder`
- Permission：Contents 讀寫
